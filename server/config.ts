// Конфигурация для серверного приложения
export const serverConfig = {
  // Yandex OAuth ID (публичный, не секрет)
  yandexClientId: '03e35996472d40e3b0d980943e545cb8',
  
  // VK OAuth ID (публичный, не секрет)  
  vkClientId: '54075494'
};

export default serverConfig;
